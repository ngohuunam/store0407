// export const card = state => {
//   const width = window.innerWidth
//   const xs = width < 576
//   const maxWidth = 400
//   const ratioW = 0.5
//   const ratioH = 4 / 3
//   const cWidth = xs ? width * ratioW : maxWidth
//   const pad = cWidth * 0.05
//   const iWidth = cWidth
//   const iHeight = iWidth * ratioH
//   const cHeight = iHeight * 1.1
//   const op = op(state)
//   const imgStyle = `width: ${iWidth}px; height: ${iHeight}px`
//   const cardStyle = `width: ${cWidth}px; height: ${cHeight}px; padding:${pad}px`
//   return block
// }
